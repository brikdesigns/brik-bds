---
title: Content block
description: Fixed-slot Block-layer unit — title, subtitle, description, and actions, each independently omittable.
---

import { Callout } from 'fumadocs-ui/components/callout';

ContentBlock is a fixed-slot Block-layer unit for named content with a known shape: `title` / `subtitle` / `description` / `actions`, each independently omittable. It owns the vertical rhythm BETWEEN its own slots — a tight gap ties `title` to `subtitle`, a medium gap separates the last text slot from `description`, and the same medium gap leads into `actions`. See [ADR-023](https://github.com/brikdesigns/brik-bds/blob/main/docs/adrs/ADR-023-content-blocks-and-prose-rhythm.md).

ContentBlock never owns layout. Column count and orientation come from a Layout primitive ([Stack](/docs/components/stack), [Grid](/docs/components/grid)) or a Container ([Card](/docs/components/card)) wrapped around it.

<ComparisonGrid
  items={[
    {
      title: 'ContentBlock',
      whenToUse: 'Named content with a known shape — title / subtitle / description / actions, each omittable.',
      whenNotToUse: 'Free-form rich text with no slot shape (Prose), or a centered section intro (SectionHeader).',
      code: `<ContentBlock title="…" description="…" />`,
    },
    {
      title: 'Prose',
      whenToUse: 'Open-ended, already-sanitized rich text (CMS HTML) with no fixed slots.',
      whenNotToUse: 'The content splits cleanly into title / description / actions — that\'s ContentBlock.',
      code: `<Prose html={sanitized} />`,
    },
    {
      title: 'SectionHeader',
      whenToUse: 'A centered, measure-capped section intro above a grid or band — composes ContentBlock.',
      whenNotToUse: 'Inner card / CTA text with no measure or centering — use ContentBlock directly.',
      code: `<SectionHeader title="…" description="…" />`,
    },
  ]}
/>

## Use it for

- A card's text content — feature copy, testimonial framing, pricing tile copy
- A CTA's heading, supporting line, and button
- Any named content unit with a known title/subtitle/description/actions shape

For open-ended, already-sanitized rich text with no fixed slot shape, use [Prose](/docs/components/prose) instead.

## Import

```tsx
import { ContentBlock } from '@brikdesigns/bds';
```

## Variants

### All slots

```tsx
<ContentBlock
  title="Membership Plans"
  subtitle="Choose the plan that fits your practice"
  description="Every plan includes unlimited support and quarterly reviews."
  actions={<Button variant="primary">View Plans</Button>}
/>
```

### Omitted slots

Any slot can be dropped independently — the rhythm collapses to whatever slots remain, with no leftover spacing from an omitted one.

```tsx
<ContentBlock
  title="First Impressions"
  description="75% of website credibility comes from design."
/>
```

### Title element level

`title` renders as `titleAs` (default `h3` — the common case nests inside a `Card`). Pick the element by outline position, not by convenience.

```tsx
// Direct outline sibling of the page's <h1>
<ContentBlock titleAs="h2" title="Membership Plans" />

// Decorative unit repeated in a grid — not an outline node
<ContentBlock titleAs="div" title="Feature name" />
```

<Callout type="info">
  See [HTML Semantics — Heading element selection](/docs/build-standards/html-semantics#heading-element-selection) for the full rule.
</Callout>

### On a filled brand band

`onColor` swaps `title` / `subtitle` / `description` to `--text-on-color-dark` for a block on a filled brand or dark surface. The block owns its own inverse story ([ADR-023 amendment 2026-08-16](https://github.com/brikdesigns/brik-bds/blob/main/docs/adrs/ADR-023-content-blocks-and-prose-rhythm.md)) — never push a `color` override from the consumer into a slot the block renders. `--text-on-color-dark` is mode-invariant white, so one prop covers light and dark with no theme branch.

```tsx
<ContentBlock
  onColor
  title="Get in touch"
  description="Starting a new project or want to collaborate with us?"
  actions={<Button variant="on-color">Let&apos;s Talk</Button>}
/>
```

<Callout type="warn">
  White on `--surface-brand-primary` is **3.78:1 — AA-large (3:1), not AA (4.5:1)**. Brand-primary fills are gated AA-large by policy (see [Color Pairings](/docs/foundation/color-pairings), BDS-22 / ADR-015). The `title` is large text and unaffected; keep a band `description` short rather than running body copy long on the fill. Long-form copy belongs on a neutral surface.
</Callout>

For a *centered, measure-capped* section intro on a band, use [SectionHeader](/docs/components/section-header) — it forwards `onColor`.

## Props

{/* props-check: ContentBlockProps @ components/ui/ContentBlock/ContentBlock.tsx */}

| Prop | Type | Default |
| --- | --- | --- |
| `title` | `ReactNode` | — |
| `subtitle` | `ReactNode` | — |
| `description` | `ReactNode` | — |
| `actions` | `ReactNode` | — |
| `titleAs` | `'h1' \| 'h2' \| 'h3' \| 'h4' \| 'div' \| 'p'` | `'h3'` |
| `size` | `'sm' \| 'md' \| 'lg'` | `'sm'` |
| `onColor` | `boolean` | `false` |

## Related

- [Prose](/docs/components/prose) — the free-form rich-text counterpart
- [Page Structure — Content blocks and prose](/docs/build-standards/page-structure#content-blocks-and-prose) — the spacing map both components share
