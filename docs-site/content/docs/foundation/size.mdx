---
title: Size
description: Dimensional constraints for containers, breakpoints, and elements.
---

import { sizeScale } from '@bds-tokens';

Size tokens define dimensional constraints — how big things can be, where the layout breaks, and how wide content columns sit within a viewport.

## Size scale

<SpacingScale scale={sizeScale} prefix="--size" />

## Content widths

Max-width values for content containers. Apply to wrapper elements with `max-width` to prevent lines from growing too wide at large viewport sizes.

| Token | CSS Variable | Value | Use for |
|---|---|---|---|
| Narrow | `--content-width-sm` | 640px | Prose, reading columns, focused forms |
| Default | `--content-width-md` | 800px | Standard body sections |
| Wide | `--content-width-lg` | 1024px | Feature sections, content grids |
| XL | `--content-width-xl` | 1280px | Hero bands, wide layouts |
| Full | `--content-width-full` | 100% | Full-bleed (use sparingly) |

```css
.content-section {
  max-width: var(--content-width-md);
  margin-inline: auto;
}
```

## Line length

`--measure-*` caps a text column's width in `ch` units so a line stays readable as the type scales. Unlike `--content-width-*` — a `px` ceiling on a container — a measure tracks the font size, so the column holds the same *number of characters* at any size. Apply it to the text element itself, not its wrapper.

| Token | CSS Variable | Value | Use for |
|---|---|---|---|
| Small | `--measure-sm` | 44ch | Captions, sidenotes, narrow columns |
| Medium | `--measure-md` | 60ch | Body prose — the default reading measure |
| Large | `--measure-lg` | 72ch | Long-form articles, wide reading columns |

```css
.article-body {
  max-width: var(--measure-md);
}
```

## Screen breakpoints

BDS defines five canonical breakpoints. The CSS custom properties (`--breakpoint-*`) are provided for Figma/documentation reference. **For CSS `@media` rules, import the `breakpoints` constant from `@bds-tokens`** — CSS custom properties cannot be used inside `@media()`.

| Name | CSS Variable | Value |
|---|---|---|
| Mobile | `--breakpoint-mobile` | 320px |
| Tablet | `--breakpoint-tablet` | 768px |
| Desktop | `--breakpoint-desktop` | 1024px |
| Wide | `--breakpoint-wide` | 1280px |
| Wider | `--breakpoint-wider` | 1440px |

<Callout type="warn">
`--breakpoint-web` (1200px) is a legacy Figma-sourced value kept for backward compatibility. Prefer the canonical names above in new work.
</Callout>

<Callout type="info">
  **The `--breakpoint-*` CSS custom properties are unconsumed by design.** They exist only for Figma / documentation reference — CSS variables can't be read inside `@media()`, so real breakpoint logic goes through the `breakpoints` JS constant below. That's why [Token Anatomy → Named exceptions](/docs/foundation/token-anatomy#named-exceptions) records zero `var(--breakpoint-*)` references anywhere, and flags whether BDS should ship the CSS-var family at all as an open question for the naming ADR ([#1910](https://github.com/brikdesigns/brik-bds/issues/1910)). "Prefer the canonical names" above governs the JS constant and any Figma/doc reference — not a bet that the CSS vars survive that ADR.
</Callout>

### Using breakpoints in CSS

```tsx
import { breakpoints } from '@bds-tokens';

// In a CSS-in-JS context
const styles = {
  [`@media (min-width: ${breakpoints.desktop})`]: {
    display: 'grid',
    gridTemplateColumns: '1fr 2fr',
  },
};
```

```css
/* In plain CSS — hardcode the value, comment the token name */
@media (min-width: 1024px) { /* --breakpoint-desktop */
  .sidebar-layout {
    display: grid;
  }
}
```

### Using breakpoints in JavaScript

```ts
import { breakpoints } from '@bds-tokens';

const isDesktop = window.matchMedia(
  `(min-width: ${breakpoints.desktop})`
).matches;
```

## Element sizes

Fixed element dimensions — icons, dots, indicators — use the numeric `--size-*` scale. Values step in 4px increments above 8px.

<Callout type="warn">
  **`--size-*` and `--space-*` are two scales, not one — don't treat `--size-400` and `--space-400` as interchangeable.** They share identical values on the low/mid steps (`0` through `2100`), but they diverge: `--space-*` adds half-steps (`--space-250` = 10px, `--space-350` = 14px, `--space-450` = 18px) and runs higher (to `--space-3000` = 176px), while `--size-*` tops out at `--size-2200` = 100px — and at that shared index the two disagree (`--space-2200` = 104px). Pick by intent: **`--size-*` for a fixed element dimension** (width/height of an icon, dot, indicator), **`--space-*` for layout spacing** (the primitive that `--padding-*` / `--gap-*` draw from). Verify the value in `dist/tokens.css` rather than assuming the same index matches.
</Callout>

| Token | Value |
|---|---|
| `--size-50` | 2px |
| `--size-100` | 4px |
| `--size-200` | 8px |
| `--size-400` | 16px |
| `--size-600` | 24px |
| `--size-800` | 32px |
| `--size-1000` | 40px |

The scale runs `--size-0` (0) through `--size-2200` (100px).

## Aspect ratio

`--aspect-*` feeds the `aspect-ratio` property, so a media box holds its shape as it scales — no padding-hack. Nine ratio primitives cover the common orientations, and four semantic aliases name the cases you reach for most.

| Token | CSS Variable | Ratio |
|---|---|---|
| Square | `--aspect-1-1` | 1 / 1 |
| Landscape | `--aspect-4-3` | 4 / 3 |
| Portrait | `--aspect-3-4` | 3 / 4 |
| Landscape (photo) | `--aspect-3-2` | 3 / 2 |
| Portrait (photo) | `--aspect-2-3` | 2 / 3 |
| Portrait (social) | `--aspect-4-5` | 4 / 5 |
| Widescreen | `--aspect-16-9` | 16 / 9 |
| Vertical (stories) | `--aspect-9-16` | 9 / 16 |
| Ultrawide | `--aspect-21-9` | 21 / 9 |

Four aliases point at the primitive above so intent reads at the call site — prefer them where one fits.

| Alias | Resolves to | Names |
|---|---|---|
| `--aspect-square` | `--aspect-1-1` | Avatars, thumbnails |
| `--aspect-cinema` | `--aspect-16-9` | Video, hero media |
| `--aspect-photo-landscape` | `--aspect-3-2` | Landscape photography |
| `--aspect-photo-portrait` | `--aspect-2-3` | Portrait photography |

```css
.media-frame {
  aspect-ratio: var(--aspect-cinema);
}
```

## Related

- [Spacing tokens](/docs/foundation/spacing) — the sibling `--space-*` scale for layout padding and gap (overlaps `--size-*` on the low/mid steps, diverges above — see the note above)
- [Typography → Icon sizes](/docs/foundation/typography#icon-sizes) — semantic icon-size tokens
