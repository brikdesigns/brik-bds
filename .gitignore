# MCP configuration (contains tokens)
.mcp.json

# Environment files
.env
.env.local
.env.*.local

# Node
node_modules/

# Build outputs (all regenerable)
dist/
build/
storybook-static/
build-storybook.log
*.tar.gz

# OS files
.DS_Store
logs/

# Webflow exports — keep in updates/, ignore if dumped at root
/*.html

# Archive (local reference only, not tracked)
archive/

# Local tooling
.netlify
claude-talk-to-figma-mcp/

# Claude Code local state (skills/ and references/ SHOULD be committed)
.claude/settings.local.json
.claude/workstreams.json
