name: Build Design Tokens

on:
  schedule:
    # Run daily at 9 AM UTC
    - cron: '0 9 * * *'
  push:
    paths:
      - 'updates/brik-bds.webflow/**'
  workflow_dispatch:

jobs:
  build-tokens:
    runs-on: ubuntu-latest
    permissions:
      contents: write

    steps:
    - name: Checkout code
      uses: actions/checkout@v6

    - name: Setup Node.js
      uses: actions/setup-node@v6
      with:
        node-version: '22'
        cache: 'npm'

    - name: Install dependencies
      run: npm ci

    - name: Build all tokens (Style Dictionary from Figma)
      run: npm run build:all-tokens

    - name: Check for changes
      id: check-changes
      run: |
        if git diff --quiet && [ -z "$(git ls-files --others --exclude-standard)" ]; then
          echo "no-changes=true" >> $GITHUB_OUTPUT
        else
          echo "no-changes=false" >> $GITHUB_OUTPUT
        fi

    - name: Commit and push changes
      if: steps.check-changes.outputs.no-changes == 'false'
      run: |
        git config --local user.email "action@github.com"
        git config --local user.name "GitHub Action"
        git add design-tokens/ tokens/
        git add -f build/
        git commit -m "Build design tokens — $(date -u +'%Y-%m-%d %H:%M:%S UTC')"
        git push
