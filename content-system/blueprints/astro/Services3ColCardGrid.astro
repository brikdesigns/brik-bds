---
/**
 * Services3ColCardGrid — Phase D adapter (deprecated direct path).
 *
 * After brik-bds#580, the canonical primitive is `<CardGrid>` composed
 * with consumer-supplied card markup. This file remains as an adapter
 * so the legacy `services_3col_card_grid` blueprint key continues to
 * dispatch through `BlueprintDispatcher` with the same section-data
 * contract.
 *
 * Astro consumers can't import the React `<Card preset="display">`
 * primitive — so the card markup here remains hand-rolled HTML+CSS,
 * scoped to this adapter via Astro's `<style>` block. Token pairs in
 * the embedded CSS respect the family↔size discipline:
 *   card title (h3)   — --font-family-heading + --heading-md
 *   card description  — --font-family-body + --body-md
 *
 * @deprecated Use `<CardGrid>` directly with composed slot content.
 * @summary Legacy adapter — `<CardGrid>` wrapping hand-rolled card HTML.
 */
import type { BlueprintProps } from './types';
import CardGrid from './CardGrid.astro';

interface Props extends BlueprintProps {}

const { section } = Astro.props;

const categoryLabels: Record<NonNullable<typeof section.items[number]['category']>, string> = {
  brand: 'Brand',
  marketing: 'Marketing',
  information: 'Information',
  product: 'Product',
  service: 'Back Office',
};
---

<CardGrid
  sectionKey={section.sectionKey}
  title={section.heading ?? ''}
  subtitle={section.subheading ?? undefined}
  description={section.body ?? undefined}
>
  <ul class="bp-services-grid__list" role="list">
    {section.items.map((item) => {
      const category = item.category ?? null;
      const categoryLabel = category ? categoryLabels[category] : null;
      return (
        <li class="bp-services-grid__item">
          <article
            class="bp-services-grid__card"
            data-category={category ?? undefined}
          >
            <div class="bp-services-grid__media">
              {item.imageUrl ? (
                <img
                  src={item.imageUrl}
                  alt={item.imageAlt ?? ''}
                  class="bp-services-grid__image"
                  loading="lazy"
                  decoding="async"
                />
              ) : (
                categoryLabel && (
                  <span class="bp-services-grid__media-fallback" aria-hidden="true">
                    {categoryLabel}
                  </span>
                )
              )}
              {item.hasOptions && (
                <span class="bp-services-grid__has-options">Has Options</span>
              )}
            </div>

            <div class="bp-services-grid__card-body">
              {categoryLabel && (
                <p class="bp-services-grid__category-row">
                  <span class="bp-services-grid__category-pill">
                    {categoryLabel}
                  </span>
                </p>
              )}
              <h3 class="bp-services-grid__card-title">{item.title}</h3>
              {item.description && (
                <p class="bp-services-grid__card-desc">{item.description}</p>
              )}
              {item.href && (
                <p class="bp-services-grid__cta-row">
                  <a class="bp-services-grid__cta" href={item.href}>
                    Learn more
                  </a>
                </p>
              )}
            </div>
          </article>
        </li>
      );
    })}
  </ul>
</CardGrid>

<style>
  /* Scoped to this deprecated adapter — NOT part of the bds-card-grid
     canon vocabulary. These styles go away with the adapter in Phase E.
     Token pairs respect the family↔size discipline. */

  .bp-services-grid__list {
    list-style: none;
    margin: 0;
    padding: 0;
    display: grid;
    grid-template-columns: 1fr;
    gap: var(--gap-lg);
  }

  @media (min-width: 768px) {
    .bp-services-grid__list {
      grid-template-columns: repeat(2, 1fr);
    }
  }

  @media (min-width: 992px) {
    .bp-services-grid__list {
      grid-template-columns: repeat(3, 1fr);
    }
  }

  .bp-services-grid__item {
    display: flex;
  }

  .bp-services-grid__card {
    position: relative;
    display: flex;
    flex-direction: column;
    width: 100%;
    background: var(--surface-primary);
    border: var(--border-width-md) solid var(--border-secondary);
    border-radius: var(--border-radius-md);
    overflow: hidden;
  }

  .bp-services-grid__media {
    position: relative;
    aspect-ratio: 3 / 2;
    background: var(--surface-secondary);
    display: flex;
    align-items: center;
    justify-content: center;
    overflow: hidden;
  }

  .bp-services-grid__image {
    width: 100%;
    height: 100%;
    object-fit: cover;
    display: block;
  }

  .bp-services-grid__media-fallback {
    /* Pair: heading family + heading size. */
    font-family: var(--font-family-heading);
    font-size: var(--heading-md);
    font-weight: var(--font-weight-semi-bold);
    color: var(--text-primary);
    opacity: 0.55;
    letter-spacing: 0.02em;
  }

  .bp-services-grid__has-options {
    position: absolute;
    top: var(--gap-md);
    right: var(--gap-md);
    padding: 4px 10px;
    background: var(--background-positive);
    color: var(--text-on-color-dark);
    /* Pair: label family + label size. */
    font-family: var(--font-family-label);
    font-size: var(--label-sm);
    font-weight: var(--font-weight-semi-bold);
    letter-spacing: 0.02em;
    border-radius: 999px;
    line-height: 1.2;
    z-index: 1;
  }

  .bp-services-grid__card-body {
    display: flex;
    flex-direction: column;
    gap: var(--gap-md);
    padding: var(--padding-lg);
    flex: 1 1 auto;
  }

  .bp-services-grid__category-row {
    margin: 0;
    display: flex;
  }

  .bp-services-grid__category-pill {
    /* Tag — does not stretch; sits at natural width. */
    display: inline-flex;
    align-items: center;
    align-self: flex-start;
    padding: 4px 10px;
    background: var(--surface-secondary);
    color: var(--text-primary);
    /* Pair: label family + label size. */
    font-family: var(--font-family-label);
    font-size: var(--label-md);
    font-weight: var(--font-weight-semi-bold);
    border-radius: 999px;
    line-height: 1.2;
    letter-spacing: 0.02em;
  }

  .bp-services-grid__card[data-category='brand'] .bp-services-grid__category-pill {
    background: var(--theme-yellow-yellow-light, #fff4cc);
    color: var(--theme-yellow-yellow-dark, #5b4500);
  }
  .bp-services-grid__card[data-category='marketing'] .bp-services-grid__category-pill {
    background: var(--theme-green-green-light, #d6f1da);
    color: var(--theme-green-green-dark, #1f5b2e);
  }
  .bp-services-grid__card[data-category='information'] .bp-services-grid__category-pill {
    background: var(--theme-blue-blue-light, #d6e4f5);
    color: var(--theme-blue-blue-dark, #1f3d70);
  }
  .bp-services-grid__card[data-category='product'] .bp-services-grid__category-pill {
    background: var(--theme-purple-purple-light, #ead6f5);
    color: var(--theme-purple-purple-dark, #4a1f70);
  }
  .bp-services-grid__card[data-category='service'] .bp-services-grid__category-pill {
    background: var(--theme-orange-orange-light, #ffe1cc);
    color: var(--theme-orange-orange-dark, #663300);
  }

  .bp-services-grid__card-title {
    margin: 0;
    /* Pair: heading family + heading size. */
    font-family: var(--font-family-heading);
    font-size: var(--heading-md);
    font-weight: var(--font-weight-semi-bold);
    line-height: var(--font-line-height-tight);
    color: var(--text-primary);
  }

  .bp-services-grid__card-desc {
    margin: 0;
    /* Pair: body family + body size. */
    font-family: var(--font-family-body);
    font-size: var(--body-md);
    line-height: var(--font-line-height-normal);
    color: var(--text-primary);
  }

  .bp-services-grid__cta-row {
    margin: 0;
    margin-top: auto;
  }

  .bp-services-grid__cta {
    /* Astro adapter "Learn more" — primary-button styling without the
       React `<LinkButton variant="primary">` we'd use in the React adapter. */
    display: inline-flex;
    align-items: center;
    gap: var(--gap-sm);
    padding: var(--padding-sm) var(--padding-lg);
    background: var(--background-brand-primary);
    color: var(--text-on-brand-primary, var(--text-on-color-light));
    text-decoration: none;
    border-radius: var(--border-radius-sm);
    /* Pair: label family + label size. */
    font-family: var(--font-family-label);
    font-size: var(--label-md);
    font-weight: var(--font-weight-semi-bold);
    line-height: 1.4;
  }

  .bp-services-grid__cta:hover {
    filter: brightness(0.95);
  }

  .bp-services-grid__cta:focus-visible {
    outline: 2px solid var(--border-focus, var(--text-brand-primary));
    outline-offset: 2px;
  }
</style>
