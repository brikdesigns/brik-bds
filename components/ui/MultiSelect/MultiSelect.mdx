import { Meta, Canvas, ArgTypes } from '@storybook/addon-docs/blocks';
import * as Stories from './MultiSelect.stories';

<Meta of={Stories} />

# Multi-select

A select dropdown that supports multiple selections. Selected items appear as removable `Tag` chips below the dropdown. Composes `Select` + `Tag`.

---

## Playground

<Canvas of={Stories.Playground} />

## Variants

Sizes, disabled state, and error validation.

<Canvas of={Stories.Variants} />

## Patterns

Project intake form with multiple multi-select fields.

<Canvas of={Stories.Patterns} />

---

## Props

<ArgTypes of={Stories} />

---

## Usage

```tsx
import { MultiSelect } from '@brik/bds';

const [selected, setSelected] = useState<string[]>([]);

<MultiSelect
  label="Services"
  placeholder="Select services..."
  options={[
    { label: 'Brand Design', value: 'brand' },
    { label: 'Marketing', value: 'marketing' },
    { label: 'Product Design', value: 'product' },
  ]}
  value={selected}
  onChange={setSelected}
/>
```
