import { Meta, Canvas, ArgTypes } from '@storybook/addon-docs/blocks';
import { ComponentLinks } from '../../../.storybook/blocks/ComponentLinks';
import * as Stories from './MultiSelect.stories';

<Meta of={Stories} />

# Multi-select

<ComponentLinks slug="multi-select" />

Multi-select dropdown that composes [`Select`](/?path=/docs/components-form-select--docs) for the picker and [`Tag`](/?path=/docs/components-indicator-tag--docs) for selected items. Picking an option adds a removable Tag chip below; selected options are filtered out of the dropdown until removed.

## Playground

<Canvas of={Stories.Default} />

## Variants

MultiSelect has no semantic-variant axis — all variation (size, tagSize, error, disabled, helper text, fullWidth) is exposed via Controls on the canvas above. See [ADR-010 §components without a variant axis](../../../docs/adrs/ADR-010-storybook-axes-of-information.md) for the framework.

<Canvas of={Stories.Default} />

- **`sm`** — 32px Select + small Tag chips
- **`md`** — 40px Select + medium Tag chips (default)
- **`lg`** — 48px Select + large Tag chips

`tagSize` defaults to matching `size` but can be overridden independently.

## Props

<ArgTypes of={Stories} />

## Notes

> **Note** — Selected options are filtered out of the dropdown until removed. When all options are selected, the placeholder switches to "All options selected" and the dropdown is auto-disabled.

> **Note** — Composes `Select` and `Tag`. Visual treatment for both follows their respective gold-standard pages — fixes to either propagate here automatically.
