import { Meta, Canvas, ArgTypes } from '@storybook/addon-docs/blocks';
import { ComponentLinks } from '../../../.storybook/blocks/ComponentLinks';
import * as Stories from './EmptyState.stories';

<Meta of={Stories} />

# Empty state

<ComponentLinks slug="empty-state" />

Placeholder for screens with no data. Use when a list, table, or page has no content to display, guiding users toward their next action.

## Default

<Canvas of={Stories.Default} />

## Variants

Action and description presence are Controls on any story. These two show the composed slots — a media illustration and a custom children affordance — that args alone can't express.

### With media

<Canvas of={Stories.WithMedia} />

### With custom content

<Canvas of={Stories.WithCustomContent} />

## Props

<ArgTypes of={Stories} />
