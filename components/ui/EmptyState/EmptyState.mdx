import { Meta, Canvas, ArgTypes } from '@storybook/addon-docs/blocks';
import { ComponentLinks } from '../../../.storybook/blocks/ComponentLinks';
import * as Stories from './EmptyState.stories';

<Meta of={Stories} />

# Empty state

<ComponentLinks slug="empty-state" />

Placeholder for screens with no data. Use when a list, table, or page has no content to display, guiding users toward their next action.

## Playground

<Canvas of={Stories.Playground} />

## Variants

The component varies on whether a primary action is present, how much content the title and description carry, and whether the children slot replaces the button. Each axis is its own story.

### With action

<Canvas of={Stories.WithAction} />

### No action

<Canvas of={Stories.NoAction} />

### Title only

<Canvas of={Stories.TitleOnly} />

### With custom content

<Canvas of={Stories.WithCustomContent} />

## Patterns

Real-world compositions — three canonical empty-state moments product teams hit repeatedly.

### Inbox zero

<Canvas of={Stories.InboxZero} />

### First-time user

<Canvas of={Stories.FirstTimeUser} />

### No search results

<Canvas of={Stories.NoSearchResults} />

## Props

<ArgTypes of={Stories} />
