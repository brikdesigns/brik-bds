import { Meta, Canvas, ArgTypes } from '@storybook/addon-docs/blocks';
import * as Stories from './TagList.stories';

<Meta of={Stories} />

# TagList

Horizontal cluster of `<Tag>` elements with locked spacing. Replaces the `<div style={{ display: 'flex', gap, flexWrap }}>` wrapper that had grown around tag arrays across the portal.

Use as a `Field` value, inside a table cell, or standalone inside a `SheetSection`.

---

## Playground

<Canvas of={Stories.Playground} />

## Gaps

Three locked gap sizes — `xs` for tight clusters, `sm` for standard lists, `md` for roomy displays.

<Canvas of={Stories.Gaps} />

## Inside a Field

The canonical use: a `Field` value expressed as tagged categories.

<Canvas of={Stories.InsideField} />

## Patterns

Several fields each rendering a tag cluster — the recurring "brand personality / voice / industries" pattern across onboarding sheets.

<Canvas of={Stories.Patterns} />

---

## Props

<ArgTypes of={Stories} />

---

## Usage

```tsx
import { TagList, Tag, Field } from '@brikdesigns/bds';

<Field label="Services offered">
  <TagList>
    <Tag size="sm">Cosmetic</Tag>
    <Tag size="sm">General</Tag>
    <Tag size="sm">Implants</Tag>
  </TagList>
</Field>
```

## Rules

- **Don't wrap a single Tag.** If there's only one, just use `<Tag>` directly inside the Field.
- **Use `Tag` children, not arbitrary nodes.** TagList locks spacing for Tag's specific footprint; other content will misalign.
