import { Meta, Canvas, ArgTypes } from '@storybook/addon-docs/blocks';
import * as Stories from './Divider.stories';

<Meta of={Stories} />

# Divider

Visual separator for content sections. Supports horizontal and vertical orientations with configurable spacing.

---

## Playground

Use the Controls panel to explore all props interactively.

<Canvas of={Stories.Playground} />

## Orientations

Two orientations share the same component.

<Canvas of={Stories.Horizontal} />
<Canvas of={Stories.Vertical} />

## Spacings

Side-by-side comparison of all four spacing values.

<Canvas of={Stories.Spacings} />

- **`none`** -- 0px. Tight separators, table rows.
- **`sm`** -- 12px. Compact spacing.
- **`md`** -- 16px. Default breathing room.
- **`lg`** -- 24px. Section-level separation.

---

## Props

<ArgTypes of={Stories} />

---

## Usage

```tsx
import { Divider } from '@brik/bds';

// Basic horizontal divider
<Divider />

// With spacing
<Divider spacing="lg" />

// Vertical in a flex row
<div style={{ display: 'flex', alignItems: 'center' }}>
  <span>Left</span>
  <Divider orientation="vertical" spacing="md" />
  <span>Right</span>
</div>
```
