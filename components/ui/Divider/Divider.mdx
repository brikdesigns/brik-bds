import { Meta, Canvas, ArgTypes } from '@storybook/addon-docs/blocks';
import { ComponentLinks } from '../../../.storybook/blocks/ComponentLinks';
import * as Stories from './Divider.stories';

<Meta of={Stories} />

# Divider

<ComponentLinks slug="divider" />

Visual separator for content sections. Supports horizontal and vertical orientations with configurable spacing.

## Default

<Canvas of={Stories.Default} />

## Props

<ArgTypes of={Stories} />

## Notes

- `spacing` scale: `none` 0px for tight separators and table rows, `sm` 12px compact, `md` 16px default breathing room, `lg` 24px section-level separation. Orientation and spacing are Controls on the Default sandbox.
