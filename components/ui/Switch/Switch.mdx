import { Meta, Canvas, ArgTypes } from '@storybook/blocks';
import * as Stories from './Switch.stories';

<Meta of={Stories} />

# Switch

Toggle control for binary on/off states. Use for settings, feature flags, and preferences where immediate state change is expected.

## Overview

<Canvas of={Stories.Default} />

## Sizes

Three sizes matching the Figma spec: large (56x32), medium (32x18), and small (28x16).

### Unchecked
<Canvas of={Stories.Sizes} />

### Checked
<Canvas of={Stories.SizesChecked} />

## Variants

### Checked
Default checked state.
<Canvas of={Stories.Checked} />

### Disabled
Prevent interaction with the switch.
<Canvas of={Stories.Disabled} />

### Without label
Use when the switch context is clear from surrounding content.
<Canvas of={Stories.WithoutLabel} />

## Controlled mode

Manage switch state externally for dynamic behavior.
<Canvas of={Stories.Controlled} />

## Usage examples

### Settings panel
Multiple switches in a preferences panel.
<Canvas of={Stories.SettingsPanel} />

### Compact settings
Use `sm` size for dense UI like editor preferences.
<Canvas of={Stories.CompactSettings} />

## Props

<ArgTypes of={Stories} />

## Usage

```tsx
import { Switch } from '@brik/bds';
import { useState } from 'react';

// Uncontrolled
<Switch label="Enable feature" defaultChecked />

// Controlled
const [enabled, setEnabled] = useState(false);
<Switch
  label="Enable notifications"
  checked={enabled}
  onChange={(e) => setEnabled(e.target.checked)}
/>

// Size variants
<Switch size="lg" label="Large toggle" />
<Switch size="md" label="Medium toggle" />
<Switch size="sm" label="Small toggle" />
```
