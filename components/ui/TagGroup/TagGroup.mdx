import { Meta, Canvas, ArgTypes } from '@storybook/addon-docs/blocks';
import { ComponentLinks } from '../../../.storybook/blocks/ComponentLinks';
import * as Stories from './TagGroup.stories';
import { TagGroup } from './TagGroup';
import { Tag } from '../Tag';
import { Field } from '../Field';

{/* Gap-axis comparison — docs-local, not a story export: `gap` is a Control on
    Default, and a side-by-side gallery is an MDX concern (#1489). */}
export const Frame = ({ width = '360px', children }) => (
  <div style={{ width, padding: 'var(--padding-lg)', background: 'var(--surface-primary)' }}>
    {children}
  </div>
);

export const GapScale = () => (
  <Frame>
    <div style={{ display: 'flex', flexDirection: 'column', gap: 'var(--gap-xl)' }}>
      <TagGroup gap="xs">
        <Tag size="sm">xs gap</Tag>
        <Tag size="sm">tight</Tag>
        <Tag size="sm">cluster</Tag>
      </TagGroup>
      <TagGroup gap="sm">
        <Tag size="sm">sm gap</Tag>
        <Tag size="sm">standard</Tag>
        <Tag size="sm">list</Tag>
      </TagGroup>
      <TagGroup gap="md">
        <Tag size="md">md gap</Tag>
        <Tag size="md">roomy</Tag>
        <Tag size="md">display</Tag>
      </TagGroup>
    </div>
  </Frame>
);

{/* Cross-component composition (TagGroup as a Field value) — docs-local per
    #2499 / Rule 4, not a TagGroup API exercise. */}
export const InsideFieldDemo = () => (
  <Frame>
    <Field label="Services offered">
      <TagGroup>
        <Tag size="sm">Cosmetic</Tag>
        <Tag size="sm">General</Tag>
        <Tag size="sm">Implants</Tag>
        <Tag size="sm">Invisalign</Tag>
        <Tag size="sm">Whitening</Tag>
      </TagGroup>
    </Field>
  </Frame>
);

<Meta of={Stories} />

# TagGroup

<ComponentLinks slug="tag-group" />

Horizontal cluster of `<Tag>` elements with locked spacing. Use as a `Field` value, inside a table cell, or standalone inside a `SheetSection`.

## Default

<Canvas of={Stories.Default} />

## Variants

### Gaps

Three locked gap sizes — `xs` for tight clusters, `sm` for standard lists, `md` for roomy displays.

<Canvas>
  <GapScale />
</Canvas>

## Patterns

### Inside a Field

The canonical use: a `Field` value expressed as tagged categories.

<Canvas>
  <InsideFieldDemo />
</Canvas>

## Props

<ArgTypes of={Stories} />
