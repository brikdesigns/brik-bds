import { Meta, Canvas, ArgTypes } from '@storybook/addon-docs/blocks';
import { ComponentLinks } from '../../../.storybook/blocks/ComponentLinks';
import * as Stories from './Counter.stories';

<Meta of={Stories} />

# Counter

<ComponentLinks slug="counter" />

Numeric count indicator with status colors. Pill-shaped badge for notification counts, item quantities, and step indicators.

## Default

<Canvas of={Stories.Default} />

## Props

<ArgTypes of={Stories} />

## Notes

- `status` and `size` are pure color/dimension axes driven by Controls. Status semantics: `success` completed items / `error` alerts / `warning` pending / `neutral` inactive / `progress` in-flight / `brand` general counts and notifications.
