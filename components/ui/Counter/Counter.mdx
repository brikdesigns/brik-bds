import { Meta, Canvas, ArgTypes } from '@storybook/addon-docs/blocks';
import * as Stories from './Counter.stories';

<Meta of={Stories} />

# Counter

Numeric count indicator with status colors. Pill-shaped badge for notification counts, item quantities, and step indicators.

---

## Playground

Use the Controls panel to explore all props interactively.

<Canvas of={Stories.Playground} />

## Variants

All statuses across all sizes, plus max overflow behavior.

<Canvas of={Stories.Variants} />

- **`success`** -- Completed items, positive counts.
- **`error`** -- Error counts, alerts.
- **`warning`** -- Pending items, attention needed.
- **`neutral`** -- Inactive counts, disabled.
- **`progress`** -- In-progress items.
- **`brand`** -- General counts, notifications.

## Patterns

Real-world notification count compositions.

<Canvas of={Stories.Patterns} />

---

## Props

<ArgTypes of={Stories} />

---

## Usage

```tsx
import { Counter } from '@brik/bds';

// Basic counter
<Counter count={5} status="success" />

// Sizes
<Counter count={3} status="error" size="md" />

// Max overflow
<Counter count={150} max={99} status="progress" size="lg" />
```
