import { Meta, Canvas, ArgTypes } from '@storybook/addon-docs/blocks';
import { ComponentLinks } from '../../../.storybook/blocks/ComponentLinks';
import * as Stories from './DataView.stories';

<Meta of={Stories} />

# DataView

<ComponentLinks slug="data-view" />

A family of four thin shells — `TableView`, `ListView`, `ProfileView`, `BoardView` — that own the loading, empty, and error states every data display needs, so consumers stop rebuilding them per page. Pass the display primitive (`Table`, `CardList`, a `DataSection` stack, a `Board`) as `children`; the view renders exactly one state in precedence order — error → loading → empty → content.

## Default

<Canvas of={Stories.Default} />

## Variants

Each named view wraps a different display primitive with the same state surface. Toggle `loading` / `empty` / `error` in Controls to see any state.

The content inside each view below is a **minimum-legible fixture** — just enough shape for the skeleton and empty state to read correctly. It is not a recommended composition; build `children` from whatever the page actually needs.

### ListView

<Canvas of={Stories.List} />

### ProfileView

<Canvas of={Stories.Profile} />

### BoardView

<Canvas of={Stories.Board} />

## Props

The same surface on every view.

<ArgTypes of={Stories} />
