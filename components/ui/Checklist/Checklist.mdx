import { Meta, Canvas, ArgTypes } from '@storybook/addon-docs/blocks';
import { ComponentLinks } from '../../../.storybook/blocks/ComponentLinks';
import * as Stories from './Checklist.stories';

<Meta of={Stories} />

# Checklist

<ComponentLinks slug="checklist" />

Row-style completion control. Combines `CompletionToggle`'s circular visual with a native `<label>` + `<input type="checkbox">`, so the **entire row is the click target**. Native `Space` / `Enter` toggle for free; no manual `onKeyDown` needed.

When checked, the label gets `text-decoration: line-through` and a muted color, and the row gains a subtle completion background.

**Distinct from `Checkbox`.** Same shape (label + box + click handler), different visual + semantics. Use `Checklist` for completion-state on units of work — task checklists, clinical procedures, compliance checks. Use `Checkbox` for form selections — multi-select filters, "I agree to terms," settings toggles. The visual difference (circle vs. square) matches the semantic difference.

For card chrome where the toggle stands alone (the rest of the card is a different click target), use `CompletionToggle` instead — `Checklist` is specifically the row composition.

## Playground

<Canvas of={Stories.Playground} />

## Variants

Each row state — empty / completed / locked / locked-and-completed — gets its own story so the autodocs page, sidebar deep-links, and Chromatic snapshots can each address one state at a time.

### Unchecked

<Canvas of={Stories.Unchecked} />

### Checked

<Canvas of={Stories.Checked} />

### Disabled

<Canvas of={Stories.Disabled} />

### Disabled checked

<Canvas of={Stories.DisabledChecked} />

## Patterns

### Daily-maintenance checklist

A canonical clinical-PMS pattern — interactive list with running `n of N completed` counter. Render-mode demo because the checklist's value is the inter-row interaction.

<Canvas of={Stories.DailyMaintenanceChecklist} />

## Props

<ArgTypes of={Stories} />
