import { Meta, Canvas, ArgTypes } from '@storybook/addon-docs/blocks';
import * as Stories from './CardControl.stories';

<Meta of={Stories} />

# Card control

Settings control card with badge, title, description, and an action element. Used for toggles, configuration options, and status displays.

## Playground

Use the Controls panel to explore all props interactively.

<Canvas of={Stories.Playground} />

## Variants

Switch actions, button actions, status badges, and minimal layout.

<Canvas of={Stories.Variants} />

## Patterns

Settings panel with mixed action types.

<Canvas of={Stories.Patterns} />

## Props

<ArgTypes of={Stories} />

## Usage

```tsx
import { CardControl, Badge, Switch, Button } from '@brik/bds';

<CardControl
  title="Notifications"
  description="Receive email notifications for updates"
  badge={<Badge size="xs" status="positive" icon={<CheckIcon />} />}
  action={<Switch checked={enabled} onChange={handleChange} />}
/>
```
