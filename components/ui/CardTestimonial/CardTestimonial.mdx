import { Meta, Canvas, ArgTypes } from '@storybook/addon-docs/blocks';
import { ComponentLinks } from '../../../.storybook/blocks/ComponentLinks';
import * as Stories from './CardTestimonial.stories';

<Meta of={Stories} />

# Card testimonial

<ComponentLinks slug="card-testimonial" />

Customer testimonial card with quote, attribution, and optional star rating.

> **Note** — Deprecated. Use `<Testimonial>` instead ([#702](https://github.com/brikdesigns/brik-bds/issues/702)) — the renamed successor with the same props. The export name and import path are unchanged until removal.

## Playground

<Canvas of={Stories.Default} />

## Variants

### Outlined

`variant="outlined"` swaps the brand-tinted surface for a neutral background with border. Use on light-background sections where the brand fill would compete with surrounding content.

<Canvas of={Stories.Outlined} />

## Props

<ArgTypes of={Stories} />
