import { Meta, Canvas, ArgTypes } from '@storybook/addon-docs/blocks';
import { ComponentLinks } from '../../../.storybook/blocks/ComponentLinks';
import * as Stories from './BulletList.stories';
import { BulletList } from './BulletList';
import { Field } from '../Field';

{/* Marker-axis comparison — docs-local, not a story export: `marker` is a
    Control on Default, and a side-by-side gallery is an MDX concern (#1489). */}
export const MarkerScale = () => (
  <div style={{ display: 'flex', flexDirection: 'column', gap: 'var(--gap-xl)' }}>
    <BulletList marker="disc" items={['First disc item', 'Second disc item', 'Third disc item']} />
    <BulletList marker="decimal" items={['First numbered item', 'Second numbered item', 'Third numbered item']} />
    <BulletList marker="none" items={['No marker row one', 'No marker row two', 'No marker row three']} />
  </div>
);

{/* Cross-component composition (BulletList as a Field value) — docs-local per
    #2499 / Rule 4, not a BulletList API exercise. */}
export const InsideFieldDemo = () => (
  <div style={{ width: '360px', padding: 'var(--padding-lg)', background: 'var(--surface-primary)' }}>
    <Field label="Anti-messages">
      <BulletList
        items={[
          'No price-first positioning',
          'No corporate-clinic language',
          'Avoid dental-industry jargon',
        ]}
      />
    </Field>
  </div>
);

<Meta of={Stories} />

# BulletList

<ComponentLinks slug="bullet-list" />

Structured list of short text items — anti-messages, proof points, approved CTAs, brand rules. Pass `marker="decimal"` for numbered lists; the element switches to `<ol>` automatically.

## Default

<Canvas of={Stories.Default} />

## Variants

### Markers

Three marker styles — `disc` (default), `decimal`, `none`. Use the Controls panel on Default to try `density` (`compact` / `comfortable`).

<Canvas>
  <MarkerScale />
</Canvas>

## Patterns

The canonical composition: a `Field` value expressed as a short bullet list.

<Canvas>
  <InsideFieldDemo />
</Canvas>

## Props

<ArgTypes of={Stories} />
