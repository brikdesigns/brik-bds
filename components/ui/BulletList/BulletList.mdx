import { Meta, Canvas, ArgTypes } from '@storybook/addon-docs/blocks';
import { ComponentLinks } from '../../../.storybook/blocks/ComponentLinks';
import * as Stories from './BulletList.stories';

<Meta of={Stories} />

# BulletList

<ComponentLinks slug="bullet-list" />

Structured list of short text items — anti-messages, proof points, approved CTAs, brand rules. Pass `marker="decimal"` for numbered lists; the element switches to `<ol>` automatically.

## Playground

<Canvas of={Stories.Playground} />

## Variants

### Markers

Three marker styles — `disc` (default), `decimal`, `none`.

<Canvas of={Stories.Markers} />

### Density

`compact` for tight stat-like lists, `comfortable` (default) for content lists.

<Canvas of={Stories.Density} />

### Inside a Field

The canonical use: a `Field` value expressed as a short bullet list.

<Canvas of={Stories.InsideField} />

## Patterns

Brand rules and proof-points across a sheet body.

<Canvas of={Stories.Patterns} />

## Props

<ArgTypes of={Stories} />
