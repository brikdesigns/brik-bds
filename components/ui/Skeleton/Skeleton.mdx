import { Meta, Canvas, ArgTypes } from '@storybook/addon-docs/blocks';
import { ComponentLinks } from '../../../.storybook/blocks/ComponentLinks';
import * as Stories from './Skeleton.stories';

<Meta of={Stories} />

# Skeleton

<ComponentLinks slug="skeleton" />

Content placeholder with shimmer animation for loading states. Maintains layout and reduces perceived load time.

## Default

<Canvas of={Stories.Default} />

## Props

<ArgTypes of={Stories} />

## Notes

- `variant` shapes: `text` (100% × 1em) for text-line placeholders, `circular` (40px) for avatars and icons, `rectangular` (100% × 140px) for images, cards, and sections — driven by the Control on the Default sandbox.
