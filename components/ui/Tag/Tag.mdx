import { Meta, Canvas, ArgTypes } from '@storybook/addon-docs/blocks';
import { ComponentLinks } from '../../../.storybook/blocks/ComponentLinks';
import { Icon } from '@iconify/react';
import * as Stories from './Tag.stories';
import { Tag } from './Tag';
import { Badge } from '../Badge/Badge';

{/* Axis comparisons — appearance / size / density. Docs-local, not story
    exports: each axis is a Control on Default, and a side-by-side gallery is
    an MDX concern (#1489). Same rationale as BadgeTagAlignmentDemo below. */}
export const AxisRow = ({ children }) => (
  <div style={{ display: 'flex', gap: 'var(--gap-md)', flexWrap: 'wrap', alignItems: 'center' }}>{children}</div>
);

export const AppearanceScale = () => (
  <AxisRow>
    <Tag appearance="solid">Solid</Tag>
    <Tag appearance="subtle">Subtle</Tag>
    <Tag appearance="muted">Muted</Tag>
  </AxisRow>
);

export const SizeScale = () => (
  <AxisRow>
    <Tag size="xs" icon={<Icon icon="ph:tag" />} />
    <Tag size="sm">Small</Tag>
    <Tag size="md">Medium</Tag>
    <Tag size="lg">Large</Tag>
  </AxisRow>
);

export const DensityScale = () => (
  <div style={{ display: 'flex', flexDirection: 'column', gap: 'var(--gap-lg)' }}>
    {['sm', 'md', 'lg'].map((size) => (
      <AxisRow key={size}>
        <span style={{ fontFamily: 'var(--font-family-label)', fontSize: 'var(--body-xs)', color: 'var(--text-muted)', width: '24px' }}>{size}</span>
        <Tag size={size} density="comfortable">Comfortable</Tag>
        <Tag size={size} density="compact">Compact</Tag>
      </AxisRow>
    ))}
  </div>
);

{/* Cross-component demo — Badge + Tag share the size scale. Lives here (docs),
    not as a Tag story, because it isn't part of Tag's own API surface
    (storybook-story-shape consolidation rule 4). Mirrors the Badge.mdx twin. */}
export const BadgeTagAlignmentDemo = () => (
  <div style={{ display: 'flex', flexDirection: 'column', gap: 'var(--gap-xl)' }}>
    {['xs', 'sm', 'md', 'lg'].map((size) => (
      <div key={size} style={{ display: 'flex', gap: 'var(--gap-md)', flexWrap: 'wrap', alignItems: 'center' }}>
        <span style={{ fontFamily: 'var(--font-family-label)', fontSize: 'var(--body-xs)', color: 'var(--text-muted)', width: 'var(--size-600)' }}>{size}</span>
        {size === 'xs' ? (
          <>
            <Badge size="xs" status="positive" icon={<Icon icon="ph:star" />} />
            <Tag size="xs" icon={<Icon icon="ph:tag" />} />
          </>
        ) : (
          <>
            <Badge size={size} status="positive" icon={<Icon icon="ph:star" />}>Active</Badge>
            <Tag size={size} icon={<Icon icon="ph:tag" />}>Category</Tag>
          </>
        )}
      </div>
    ))}
  </div>
);

<Meta of={Stories} />

# Tag

<ComponentLinks slug="tag" />

Categorization labels for classifying records — industry, segment, owner, size. Available in three sizes (`sm`, `md`, `lg`) plus `xs` icon-only.

## Default

<Canvas of={Stories.Default} />

## Variants

**Appearance** (shared axis with Badge and Chip — see [Vocabulary › Axes](https://design.brikdesigns.com/docs/primitives/naming-conventions)) and icon slots are exposed via Controls on Default:

- **`solid`** (default) — Neutral filled background. Matches existing Tag styling.
- **`subtle`** — Transparent background with a hairline border. Lower visual weight when Tags sit on a colored surface.
- **`muted`** — Quiet neutral fill for a low-emphasis category label, using the same AA-cleared pairing as Badge `info`. **Boundary:** `muted` is a quiet neutral label, not a status — for semantic status (positive/warning/error/etc.) use `Badge` instead.

Below: appearance, sizes, and density — axes where a side-by-side comparison is clearer than toggling Controls.

### Appearance

<Canvas>
  <AppearanceScale />
</Canvas>

### Sizes

<Canvas>
  <SizeScale />
</Canvas>

### Density

`comfortable` (default) vs `compact` — compact tightens horizontal padding one token-step down per size so more Tags fit inline in a dense table row. Height is unchanged (shared axis with Badge, BulletList, Table).

<Canvas>
  <DensityScale />
</Canvas>

## Patterns

### Badge + Tag alignment

Badge and Tag share the same height at every size tier — useful when mixing them inline.

<Canvas>
  <BadgeTagAlignmentDemo />
</Canvas>

## Props

<ArgTypes of={Stories} />

