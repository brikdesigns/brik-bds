import { Meta, Canvas, ArgTypes } from '@storybook/addon-docs/blocks';
import * as Stories from './Tag.stories';

<Meta of={Stories} />

# Tag

Categorization and filtering labels with optional left/right icons and remove functionality. Available in three sizes (`sm`, `md`, `lg`).

---

## Playground

Use the Controls panel to explore all props interactively.

<Canvas of={Stories.Playground} />

## Variants

Sizes, icon positions, and states in one view.

<Canvas of={Stories.Variants} />

## Patterns

Real-world compositions for categorization and filtering.

<Canvas of={Stories.Patterns} />

---

## Props

<ArgTypes of={Stories} />

---

## Usage

```tsx
import { Tag } from '@brik/bds';
import { FontAwesomeIcon } from '@fortawesome/react-fontawesome';
import { faCertificate, faCircleXmark } from '@fortawesome/free-solid-svg-icons';

// Basic tag
<Tag>Category</Tag>

// Sizes
<Tag size="sm">Small</Tag>
<Tag size="md">Medium</Tag>
<Tag size="lg">Large</Tag>

// Left icon
<Tag icon={<FontAwesomeIcon icon={faCertificate} />}>Featured</Tag>

// Right icon
<Tag trailingIcon={<FontAwesomeIcon icon={faCircleXmark} />}>Dismiss</Tag>

// Removable tag
<Tag onRemove={() => console.log('Removed')}>Filter</Tag>

// Disabled tag
<Tag disabled>Archived</Tag>
```
