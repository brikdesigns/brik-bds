import { Meta, Canvas, ArgTypes } from '@storybook/addon-docs/blocks';
import { ComponentLinks } from '../../../.storybook/blocks/ComponentLinks';
import * as Stories from './Stack.stories';
import { Stack } from './Stack';

{/* Axis comparisons — direction / gap / align. Docs-local, not story exports:
    each is a Control on Default, and side-by-side galleries are an MDX
    concern (#1489). The Box/SectionLabel helpers are gallery scaffolding, which
    the story-shape standard says never belongs in a story file. */}
export const Box = ({ children, w }) => (
  <div style={{ width: w ?? 'auto', padding: 'var(--padding-md)', background: 'var(--surface-secondary)', border: '1px dashed var(--border-secondary)', borderRadius: 'var(--border-radius-sm)', fontFamily: 'var(--font-family-body)', fontSize: 'var(--body-sm)', color: 'var(--text-primary)' }}>
    {children}
  </div>
);

export const SectionLabel = ({ children }) => (
  <div style={{ fontFamily: 'var(--font-family-label)', fontSize: 'var(--body-xs)', textTransform: 'uppercase', letterSpacing: '0.05em', color: 'var(--text-muted)', marginBottom: 'var(--gap-sm)' }}>
    {children}
  </div>
);

export const DirectionScale = () => (
  <Stack direction="vertical" gap="xl">
    <div>
      <SectionLabel>direction="vertical" (default)</SectionLabel>
      <Stack direction="vertical" gap="sm">
        <Box>Stacked top</Box>
        <Box>Stacked middle</Box>
        <Box>Stacked bottom</Box>
      </Stack>
    </div>
    <div>
      <SectionLabel>direction="horizontal"</SectionLabel>
      <Stack direction="horizontal" gap="sm">
        <Box>Left</Box>
        <Box>Center</Box>
        <Box>Right</Box>
      </Stack>
    </div>
  </Stack>
);

export const GapScaleDemo = () => (
  <Stack direction="vertical" gap="lg">
    {['tiny', 'xs', 'sm', 'md', 'lg', 'xl', 'huge'].map((g) => (
      <div key={g}>
        <SectionLabel>gap="{g}"</SectionLabel>
        <Stack direction="horizontal" gap={g}>
          <Box w="60px">A</Box>
          <Box w="60px">B</Box>
          <Box w="60px">C</Box>
        </Stack>
      </div>
    ))}
  </Stack>
);

export const AlignmentScale = () => (
  <Stack direction="vertical" gap="lg">
    {['start', 'center', 'end', 'stretch'].map((a) => (
      <div key={a}>
        <SectionLabel>align="{a}" (horizontal stack)</SectionLabel>
        <div style={{ height: 80, background: 'var(--surface-secondary)', padding: 'var(--padding-xs)' }}>
          <Stack direction="horizontal" gap="sm" align={a} style={{ height: '100%' }}>
            <Box w="60px">A</Box>
            <Box w="60px">B</Box>
            <Box w="60px">C</Box>
          </Stack>
        </div>
      </div>
    ))}
  </Stack>
);

<Meta of={Stories} />

# Stack

<ComponentLinks slug="stack" />

Stack lays out children along one axis with a consistent `gap`. Vertical by default; set `direction="horizontal"` for a row. `align` controls cross-axis alignment.

## Default

<Canvas of={Stories.Default} />

## Variants

The axes Stack varies on: `direction`, the `gap` scale, and cross-axis `align`.

### Directions

<Canvas>
  <DirectionScale />
</Canvas>

### Gap scale

<Canvas>
  <GapScaleDemo />
</Canvas>

### Alignment

<Canvas>
  <AlignmentScale />
</Canvas>

## Patterns

Card body — a vertical Stack composing heading, body, and action content, a composition beyond a single Stack's args.

<Canvas of={Stories.CardBody} />

## Props

<ArgTypes of={Stories} />
