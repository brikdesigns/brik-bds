import { Meta, Canvas, ArgTypes } from '@storybook/addon-docs/blocks';
import { ComponentLinks } from '../../../.storybook/blocks/ComponentLinks';
import * as Stories from './Dot.stories';

<Meta of={Stories} />

# Dot

<ComponentLinks slug="dot" />

Small status indicator circles that communicate state through color. Available in six status variants and three sizes.

## Default

<Canvas of={Stories.Default} />

## Props

<ArgTypes of={Stories} />

## Notes

- `status` semantics: `default` brand general-purpose / `positive` online-success / `warning` away-attention / `error` offline-failure / `info` in-progress / `neutral` inactive. Status and size are Controls on the Default sandbox.
