import { Meta, Canvas, ArgTypes } from '@storybook/addon-docs/blocks';
import { ComponentLinks } from '../../../.storybook/blocks/ComponentLinks';
import * as Stories from './SectionHeader.stories';
import { SectionHeader } from './SectionHeader';

<Meta of={Stories} />

export const MeasureScale = () => (
  <div style={{ display: 'flex', flexDirection: 'column', gap: 'var(--gap-xl)' }}>
    {[
      { measure: 'sm', label: 'measure="sm" (44ch)', title: 'Simple, transparent pricing', description: 'No hidden fees. Cancel anytime.' },
      { measure: 'md', label: 'measure="md" (60ch, default)', title: 'Everything your practice needs', description: 'One platform for client intake, billing, and communication — built for teams that outgrew spreadsheets.' },
      { measure: 'lg', label: 'measure="lg" (72ch)', title: 'Why practices switch to Brik', description: 'Most teams patch together a CRM, a billing tool, and a shared inbox — and still lose track of who said what to which client. Brik replaces the patchwork with one system built specifically for service businesses, so nothing falls through the cracks between tools.' },
    ].map(({ measure, label, title, description }) => (
      <div key={measure}>
        <div
          style={{
            fontFamily: 'var(--font-family-label)',
            fontSize: 'var(--body-xs)',
            textTransform: 'uppercase',
            letterSpacing: '0.05em',
            color: 'var(--text-muted)',
            marginBottom: 'var(--gap-sm)',
          }}
        >
          {label}
        </div>
        <SectionHeader measure={measure} title={title} description={description} />
      </div>
    ))}
  </div>
);

# SectionHeader

<ComponentLinks slug="section-header" />

SectionHeader is the primitive that groups a **section intro**: a centered, measure-capped `ContentBlock` sitting inside a band. It composes `ContentBlock` for the `title` / `subtitle` / `description` / `actions` rhythm (ADR-023) and adds only the horizontal concerns `ContentBlock` deliberately doesn't own (ADR-023 §5) — inline measure (`measure`, `--measure-*`, ADR-032) and column alignment (`align`).

## When to use it

- **`SectionHeader`** is the intro to a marketing or product section inside a page: centered by default, capped to a readable measure, sits in a wide band (e.g. `content-width-xl`).
- **`ContentBlock`** is the bare slot rhythm with no measure or alignment opinion; use it directly when a Layout primitive or Container already owns width and centering (e.g. inside a `Card`).
- **`PageHeader`** is the page-level header (breadcrumbs, `<h1>`, metadata, tabs) — a different layer, not a section intro.

## Default

<Canvas of={Stories.Default} />

Centered, `measure="md"` (60ch) — the default shape for a section intro. `title` renders as `titleAs` (default `h2` — section headers are outline nodes, one level under the page `<h1>`); `size` scales the title (default `lg`, the canonical section-heading size).

## Variants

`align` — `'center'` (default) centers the column with auto margins and centers text/actions; `'start'` left-anchors the column with no auto margins. Exercise it on the `Default` canvas via the `align` Control.

`measure` — the inline cap on the centered text column (`--measure-sm` 44ch / `--measure-md` 60ch / `--measure-lg` 72ch). Pick by content length, not by band width — a `measure-sm` intro can sit in an `xl` band.

<Canvas>
  <MeasureScale />
</Canvas>

`subtitle` pairs with `title` at the tight ADR-023 rhythm step — set it via the `subtitle` Control on `Default`.

`actions` centers with the rest of the column.

<Canvas of={Stories.WithActions} />

`onColor` — the CTA-band shape. Forwarded straight to `ContentBlock`, which owns the swap to `--text-on-color-dark`; SectionHeader adds no colour CSS of its own and still owns only measure + alignment. Use this instead of pushing a `color` override through `SectionHeader` into a slot it doesn't own.

<Canvas of={Stories.OnColor} />

White on `--surface-brand-primary` is **3.78:1 — AA-large (3:1), not AA (4.5:1)**; brand-primary fills are gated AA-large by policy (`tokens/contrast-pairings.json`, BDS-22 / ADR-015). The `size="lg"` title is large text; keep a band `description` short.

## Props

<ArgTypes of={Stories} />
