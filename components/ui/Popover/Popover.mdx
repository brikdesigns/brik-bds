import { Meta, Canvas, ArgTypes } from '@storybook/addon-docs/blocks';
import { ComponentLinks } from '../../../.storybook/blocks/ComponentLinks';
import * as Stories from './Popover.stories';
import { Popover } from './Popover';
import { Button } from '../Button';

{/* Placement-axis comparison — docs-local, not a story export: `placement` is
    a Control on Default, and a side-by-side gallery is an MDX concern (#1489). */}
export const DemoContent = (
  <div style={{ display: 'flex', flexDirection: 'column', gap: 'var(--gap-sm)', fontFamily: 'var(--font-family-body)', fontSize: 'var(--body-md)', color: 'var(--text-primary)' }}>
    <strong style={{ fontWeight: 'var(--font-weight-semibold)' }}>Popover title</strong>
    <p style={{ margin: 0, color: 'var(--text-secondary)' }}>
      This is some helpful content inside the popover panel.
    </p>
  </div>
);

export const PlacementScale = () => (
  <div style={{ display: 'flex', flexWrap: 'wrap', gap: 'var(--gap-xl)', alignItems: 'flex-start' }}>
    {['top', 'bottom', 'left', 'right'].map((placement) => (
      <Popover key={placement} content={DemoContent} placement={placement}>
        <Button variant="outline" size="sm">{placement[0].toUpperCase() + placement.slice(1)}</Button>
      </Popover>
    ))}
  </div>
);

<Meta of={Stories} />

# Popover

<ComponentLinks slug="popover" />

Floating content panel anchored to a trigger element. Supports click or hover triggering, four placements, and controlled/uncontrolled modes.

## Default

<Canvas of={Stories.Default} />

Trigger mode (`click`, `hover`) is a Control on the canvas above.

## Variants

### Placements

All four placements (`top`, `bottom`, `left`, `right`) side by side — the comparison is the point.

<Canvas>
  <PlacementScale />
</Canvas>

## Props

<ArgTypes of={Stories} />

