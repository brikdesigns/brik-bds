import { Meta, Canvas, ArgTypes } from '@storybook/addon-docs/blocks';
import { ComponentLinks } from '../../../.storybook/blocks/ComponentLinks';
import * as Stories from './Stepper.stories';

<Meta of={Stories} />

# Stepper

<ComponentLinks slug="stepper" />

Numeric input with increment and decrement buttons. Use for quantity selectors, pagination controls, and any setting that adjusts a number in discrete steps.

## Default

<Canvas of={Stories.Default} />

## Variants

### Sizes

`sm`, `md`, `lg` side by side. Bounds and `disabled` are available via Controls on Default.

<Canvas of={Stories.Sizes} />

## Patterns

Real-world usage: a quantity selector and a budget control with a custom step.

### Quantity selector

<Canvas of={Stories.QuantitySelector} />

### Budget control

<Canvas of={Stories.BudgetControl} />

## Props

<ArgTypes of={Stories} />

