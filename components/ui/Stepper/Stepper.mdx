import { Meta, Canvas, ArgTypes } from '@storybook/addon-docs/blocks';
import { ComponentLinks } from '../../../.storybook/blocks/ComponentLinks';
import * as Stories from './Stepper.stories';
import { Stepper } from './Stepper';

{/* Size-axis comparison — docs-local, not a story export: `size` is a Control
    on Default, and a side-by-side gallery is an MDX concern (#1489). */}
export const SizeScale = () => (
  <div style={{ display: 'flex', flexWrap: 'wrap', gap: 'var(--gap-xl)', alignItems: 'center' }}>
    <Stepper value={3} onChange={() => {}} size="sm" min={0} max={10} />
    <Stepper value={5} onChange={() => {}} size="md" min={0} max={10} />
    <Stepper value={7} onChange={() => {}} size="lg" min={0} max={10} />
  </div>
);

<Meta of={Stories} />

# Stepper

<ComponentLinks slug="stepper" />

Numeric input with increment and decrement buttons. Use for quantity selectors, pagination controls, and any setting that adjusts a number in discrete steps.

## Default

<Canvas of={Stories.Default} />

## Variants

### Sizes

`sm`, `md`, `lg` side by side. Bounds and `disabled` are available via Controls on Default.

<Canvas>
  <SizeScale />
</Canvas>

## Patterns

Real-world usage: a quantity selector with a live readout. A custom `step` is available via Controls on Default.

### Quantity selector

<Canvas of={Stories.QuantitySelector} />

## Props

<ArgTypes of={Stories} />

