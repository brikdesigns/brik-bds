import { Meta, Canvas, ArgTypes } from '@storybook/addon-docs/blocks';
import { ComponentLinks } from '../../../.storybook/blocks/ComponentLinks';
import * as Stories from './SidebarNavigation.stories';

<Meta of={Stories} />

# Sidebar navigation

<ComponentLinks slug="sidebar-navigation" />

Fixed-position primary sidebar with logo, nav items (`NavItem` internally), footer slot, and profile slot. Toggle `collapsed` for icon-only mode — pair with `SubNavigation` for a two-column app shell.

## Playground

Toggle `collapsed`, `showProfile`, `showFooterActions`, or override `width` in the Controls panel to explore states.

<Canvas of={Stories.Default} />

Pass `linkComponent` (e.g. `next/link`) so nav items route client-side instead of triggering a full-page reload — the render is visually identical to the default, so it's covered by an interaction test rather than a separate story. See [framework guides → client-side routing](/docs/getting-started/framework-guides).

## Props

<ArgTypes of={Stories} />
