import { Meta, Canvas, ArgTypes } from '@storybook/addon-docs/blocks';
import { ComponentLinks } from '../../../.storybook/blocks/ComponentLinks';
import * as Stories from './Banner.stories';

<Meta of={Stories} />

# Banner

<ComponentLinks slug="banner" />

Full-width contextual banner. `tone="announcement"` (default) renders a brand-primary surface for marketing notices; `tone="warning" | "error" | "information"` renders a secondary surface with a leading status Badge and `role="alert"`.

## Playground

<Canvas of={Stories.Playground} />

## Variants

Action, title-only, and dismissible configurations on the brand surface.

<Canvas of={Stories.Variants} />

### Status tones

Information / warning / error tones with a leading status Badge.

<Canvas of={Stories.Tones} />

## Patterns

Real-world usage: announcements and dismissible notices.

<Canvas of={Stories.Patterns} />

## Props

<ArgTypes of={Stories} />
