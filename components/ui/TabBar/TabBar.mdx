import { Meta, Canvas, ArgTypes } from '@storybook/addon-docs/blocks';
import * as Stories from './TabBar.stories';

<Meta of={Stories} />

# Tab bar

Horizontal tab navigation bar with active/inactive states. Renders a row of tab buttons styled with BDS tokens.

## Overview

<Canvas of={Stories.AllVariants} />

## Variants

### Text
Plain text labels with brand color for the active tab.
<Canvas of={Stories.Text} />

### Tab
Underline indicator with bottom border.
<Canvas of={Stories.Tab} />

### Box
Filled background for active, bordered for inactive.
<Canvas of={Stories.Box} />

### Few tabs
Compact tab bar with fewer items.
<Canvas of={Stories.FewTabs} />

### No active tab
All tabs in secondary color — useful before a selection is made.
<Canvas of={Stories.NoActive} />

### Disabled tabs
Individual tabs can be disabled.
<Canvas of={Stories.WithDisabled} />

### On dark background
Tab bar on a dark brand background using `onColor` prop.
<Canvas of={Stories.TextOnColor} />

## Props

<ArgTypes of={Stories} />

## Usage

```tsx
import { TabBar } from '@brik/bds';

// Standalone tab bar
<TabBar
  items={[
    { label: 'Overview', active: true, onClick: () => setTab('overview') },
    { label: 'Billing', onClick: () => setTab('billing') },
    { label: 'Security', onClick: () => setTab('security') },
  ]}
/>

// Composed inside PageHeader
<PageHeader
  title="My Account"
  tabs={
    <TabBar
      items={[
        { label: 'Overview', active: true },
        { label: 'Billing' },
        { label: 'Security' },
      ]}
    />
  }
/>
```

## Resources

- [Figma designs](https://www.figma.com/design/Rkdc3SIWJUdgoAkeadgZZe/%E2%9D%96-Brik-Foundations?node-id=26911-20723)
- [Design guidelines](#) — coming soon
