import { Meta, Canvas, ArgTypes } from '@storybook/addon-docs/blocks';
import { ComponentLinks } from '../../../.storybook/blocks/ComponentLinks';
import * as Stories from './PricingCard.stories';

<Meta of={Stories} />

# Pricing card

<ComponentLinks slug="pricing-card" />

> **Deprecated as a standalone surface.** `PricingCard` still ships and is still the component to use for a pricing tier — the stories below are live, not archived. Canonical guidance: [design.brikdesigns.com/docs/components/pricing-card](https://design.brikdesigns.com/docs/components/pricing-card).
>
> **The successor is [`Card` with `layout="pricing"`](?path=/docs/containers-card--docs)**, added in #2571 (ADR-038 § Amendment 2026-09-18). It carries the same anatomy — `price`, `period`, `features`, plus the shared `media` / `title` / `children` / `action` / `badge` slots. Map `image`→`media`, `description`→`children`, and `highlighted`→`variant="raised"`. `PricingCard` still ships during the migration window.

A pricing tier display card with title, price, features list, and call-to-action. Supports a highlighted state for the recommended plan.

## Playground

<Canvas of={Stories.Default} />

## Variants

### Highlighted

`highlighted=true` marks this tier as the recommended plan with brand surface treatment.

<Canvas of={Stories.Highlighted} />

## Props

<ArgTypes of={Stories} />
