import { Meta, Canvas, ArgTypes } from '@storybook/addon-docs/blocks';
import * as Stories from './PageHeader.stories';

<Meta of={Stories} />

# Page header

Composable page-level header that brings together BDS components: `Breadcrumb`, `Button`, and `TabBar`. No background — inherits from parent context.

---

## Playground

Use the Controls panel to explore all props interactively.

<Canvas of={Stories.Playground} />

## Slot shapes

Each slot is independent — use whichever ones the page needs.

- **`badge`** — `ServiceTag variant="icon"` icon badge left of title (service pages)
- **`breadcrumbs`** — `Breadcrumb` navigation trail above title
- **`actions`** — `Button` action buttons aligned right
- **`metadata`** — built-in key-value grid with border-top
- **`stats`** — summary content (e.g. `CardSummary` grid) rendered between metadata and tabs
- **`tabs`** — `TabBar` tab navigation below stats

<Canvas of={Stories.TitleOnly} />
<Canvas of={Stories.TitleWithSubtitle} />
<Canvas of={Stories.WithBreadcrumbs} />
<Canvas of={Stories.WithActions} />
<Canvas of={Stories.WithTabs} />
<Canvas of={Stories.WithBadgeAndMetadata} />
<Canvas of={Stories.WithStats} />

## Full composition

Every slot wired up — the kitchen-sink reference.

<Canvas of={Stories.FullComposition} />

---

## Props

<ArgTypes of={Stories} />

---

## Usage

```tsx
import { PageHeader, Breadcrumb, TabBar, Button, ServiceTag } from '@brik/bds';

// With service icon badge
<PageHeader
  title="Website Design"
  subtitle="Custom web development and design."
  badge={<ServiceTag category="marketing" variant="icon" serviceName="Custom Standard Web Development and Design" size="lg" />}
  breadcrumbs={<Breadcrumb items={[{ label: 'Services', href: '/' }, { label: 'Website Design' }]} />}
  actions={<Button variant="primary">Edit Service</Button>}
/>

// Without badge
<PageHeader
  title="My Account"
  subtitle="Manage your membership plan."
  tabs={<TabBar items={[{ label: 'Overview', active: true }, { label: 'Billing' }]} />}
/>
```
