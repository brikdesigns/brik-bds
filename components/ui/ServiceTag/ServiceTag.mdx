import { Meta, Canvas, ArgTypes } from '@storybook/addon-docs/blocks';
import { ComponentLinks } from '../../../.storybook/blocks/ComponentLinks';
import * as Stories from './ServiceTag.stories';

<Meta of={Stories} />

# Service tag

<ComponentLinks slug="service-tag" />

Colored indicator identifying Brik service categories. Each category has a dedicated color palette used across the Brik ecosystem for consistent service identification.

## Default

Use the Controls panel to switch `category`, `variant`, and `size`.

<Canvas of={Stories.Default} />

## Variants

ServiceTag has no dedicated per-category or per-variant stories — `category`,
`variant`, and `size` are all fully explorable via Controls on Default.

<Canvas of={Stories.Default} />

## Props

<ArgTypes of={Stories} />

## Notes

> **Note** — Migrating from the removed `ServiceBadge`, or need icon-resolution / recoloring details? See [design.brikdesigns.com/docs/components/service-tag](https://design.brikdesigns.com/docs/components/service-tag).
