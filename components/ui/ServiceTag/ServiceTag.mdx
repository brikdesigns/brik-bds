import { Meta, Canvas, ArgTypes } from '@storybook/addon-docs/blocks';
import { ComponentLinks } from '../../../.storybook/blocks/ComponentLinks';
import * as Stories from './ServiceTag.stories';
import { ServiceTag } from './ServiceTag';

{/* Axis comparison — dot variant across service lines + sizes. Docs-local,
    not a story export: `category`, `variant`, and `size` are each a Control
    on Default, and a side-by-side gallery is an MDX concern (storybook-
    story-shape standard, Rule 5). Mirrors Tag.mdx's SizeScale pattern. */}
export const DotByCategory = () => (
  <div style={{ display: 'flex', flexDirection: 'column', gap: 'var(--gap-lg)' }}>
    {['sm', 'md', 'lg'].map((size) => (
      <div key={size} style={{ display: 'flex', gap: 'var(--gap-md)', flexWrap: 'wrap', alignItems: 'center' }}>
        <span style={{ fontFamily: 'var(--font-family-label)', fontSize: 'var(--body-xs)', color: 'var(--text-muted)', width: '24px' }}>{size}</span>
        <ServiceTag category="brand" variant="dot" size={size} />
        <ServiceTag category="marketing" variant="dot" size={size} />
        <ServiceTag category="information" variant="dot" size={size} />
        <ServiceTag category="product" variant="dot" size={size} />
        <ServiceTag category="back-office" variant="dot" size={size} />
      </div>
    ))}
  </div>
);

<Meta of={Stories} />

# Service tag

<ComponentLinks slug="service-tag" />

Colored indicator identifying Brik service categories. Each category has a dedicated color palette used across the Brik ecosystem for consistent service identification.

## Default

Use the Controls panel to switch `category`, `variant`, and `size`.

<Canvas of={Stories.Default} />

## Variants

ServiceTag has no dedicated per-category or per-variant stories — `category`,
`variant`, and `size` are all fully explorable via Controls on Default.

<Canvas of={Stories.Default} />

### Dot

`dot` renders a small service-colored dot marker + label on a neutral pill —
only the marker carries the service color, unlike the fully-colored `text` /
`icon-text` pills above.

<Canvas>
  <DotByCategory />
</Canvas>

## Props

<ArgTypes of={Stories} />

## Notes

> **Note** — Migrating from the removed `ServiceBadge`, or need icon-resolution / recoloring details? See [design.brikdesigns.com/docs/components/service-tag](https://design.brikdesigns.com/docs/components/service-tag).
